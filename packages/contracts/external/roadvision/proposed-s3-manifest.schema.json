{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://roadops.example.uz/contracts/roadvision/proposed-s3-manifest.schema.json",
  "title": "RoadVision proposed S3 result manifest",
  "description": "VENDOR-PROPOSED, CONFIGURATION REQUIRED. RoadOps currently has evidence of an S3 upload workflow, but no official result-delivery contract. This manifest is a proposal for vendor approval, not a representation of an active feed.",
  "x-roadops-contract-status": "PROPOSED_VENDOR_REVIEW_REQUIRED",
  "type": "object",
  "additionalProperties": false,
  "required": ["manifest_id", "schema_version", "generated_at", "events", "manifest_sha256"],
  "properties": {
    "manifest_id": { "type": "string", "minLength": 1, "maxLength": 200 },
    "schema_version": { "type": "string", "const": "1.0.0" },
    "generated_at": { "type": "string", "format": "date-time" },
    "previous_manifest_id": { "type": ["string", "null"], "maxLength": 200 },
    "events": {
      "type": "array",
      "minItems": 1,
      "maxItems": 10000,
      "items": { "$ref": "proposed-result-event.schema.json" }
    },
    "manifest_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Vendor-defined canonicalization must be approved before this checksum is enforceable."
    }
  }
}
